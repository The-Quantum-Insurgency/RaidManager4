const { SlashCommandStringOption } = require("@discordjs/builders");
const Discord = require("discord.js");
module.exports = {
  name: "createkey",
  description: "Generates a new API Keyset",

  developer: true,

  permissions: [],

  execute: async function (RaidManager, Interaction) {},
};
